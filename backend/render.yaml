services:
  - type: web
    name: job-tracker-backend-test
    runtime: node
    plan: free
    buildCommand: npm install express
    startCommand: node health-check.js
    envVars:
      - key: PORT
        value: 10000